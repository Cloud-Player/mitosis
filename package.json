{
  "name": "mitosis",
  "version": "0.0.0",
  "scripts": {
    "install": "cd mitosis && npm install && npm run build && cd ..",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "private": true,
  "preferGlobal": true,
  "dependencies": {},
  "devDependencies": {}
}
